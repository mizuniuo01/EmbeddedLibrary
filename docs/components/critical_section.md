# critical_section

所属层级：Services。职责：表达平台注入的短临界区能力，而不暴露中断控制或 RTOS 类型。

`enter` 返回恢复 token，`exit` 必须使用对应 token 恢复进入前状态；适配器负责嵌套规则。临界区内
禁止阻塞、长 I/O、日志和未知回调。函数表和 context 由组合根拥有，并在全部调用结束前保持有效。

本组件只是校验和转发端口，不宣称主机测试能够验证具体 MCU 的中断屏蔽范围、内存屏障或 WCET。
