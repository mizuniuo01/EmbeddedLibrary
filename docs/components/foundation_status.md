# foundation_status

所属层级：Libraries

职责：提供跨基础组件复用的稳定 C API 状态码。

平台绑定方式：无。

执行上下文：任意；不访问共享可变状态。

公共接口：`foundation_status_t`，成员数值固定；后续新成员只能追加。

资源：无实例、无 RAM、无栈开销。

并发：枚举类型无内部状态。

测试：通过各使用组件的编译期数值断言和主机单元测试间接验证。
