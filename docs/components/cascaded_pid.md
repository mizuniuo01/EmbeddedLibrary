# cascaded_pid

固定两级 f32 串级 PID 组合器。组合器持有两个非拥有的 `pid_f32_t` 指针，外环输出经过独立级间闭区间限幅后
作为内环目标。输入结构体包含外环目标、外环测量、内环测量和显式 dt。

计算使用 PID 快照和恢复实现事务语义；外环或内环失败时两级状态、最近一次诊断输出和调用方输出均保持不变。
组合器不读取时钟、不创建任务、不写执行器，也不包含产品领域语义。
