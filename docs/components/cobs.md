# cobs

所属层级：Libraries。职责：提供无状态 COBS 长度计算、编码和解码。

编码结果不包含零分隔符。编码和解码先完成长度/格式验证，再写入输出；失败时不修改输出缓冲区和长度参数。

允许完全同址解码，编码及解码的其他重叠均拒绝。空 payload 编码为 `0x01`。操作 O(字节数)、无动态内存、无阻塞。

测试覆盖零字节、长块边界、非法 code、容量不足、重叠缓冲区和固定种子 round-trip。
