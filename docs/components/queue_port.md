# queue_port

固定元素大小、固定容量的并发值复制队列。调用方提供元素存储、临界区端口和等待策略；组件不拥有
元素对象，不复制指针指向的资源，也不覆盖未出队元素。普通操作支持立即返回和有限 deadline，
ISR 操作只允许立即路径并返回 `should_yield`。`stop` 清空队列并取消等待者，`start` 保留容量配置
并重新开始，`deinit` 只允许在停止后调用。
