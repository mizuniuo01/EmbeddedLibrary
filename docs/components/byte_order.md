# byte_order

所属层级：Libraries。职责：按调用方指定的 LE/BE 顺序搬运固定宽度整数。

组件不依赖平台字节序、结构体布局、RTOS 或动态内存。输入/输出均携带可验证容量；失败不修改输出值或目标缓冲区。

支持 `u16/u32/u64` 和二进制补码 `i16/i32/i64`。所有操作 O(1)、无阻塞、可重入；已初始化状态不存在。

测试覆盖黄金字节、极值、负值、容量不足、空指针和未对齐地址。
